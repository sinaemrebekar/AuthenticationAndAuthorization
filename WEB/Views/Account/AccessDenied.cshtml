@{
    ViewData["Title"] = "Erişim Reddedildi";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card text-center shadow">
                <div class="card-body">
                    <h1 class="card-title text-danger">
                        <i class="bi bi-shield-lock" style="font-size: 3rem;"></i>
                    </h1>
                    <h2 class="card-title mt-3">Erişim Reddedildi!</h2>
                    <p class="card-text mt-2">
                        Bu sayfaya erişim yetkiniz bulunmamaktadır.
                    </p>

                    <div class="d-flex justify-content-center gap-3 mt-4">
                        <a href="javascript:history.back()" class="btn btn-outline-primary">
                            <i class="bi bi-arrow-left-circle"></i> Geri Dön
                        </a>
                        <a href="@Url.Action("Index", "Home")" class="btn btn-success">
                            <i class="bi bi-house-door"></i> Ana Sayfaya Git
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
